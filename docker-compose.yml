services:
  # --- Backend Service ---
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - MODEL=${MODEL}
      - EMBEDDING_MODEL=${EMBEDDING_MODEL}
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://localhost:8000
    depends_on:
      - backend
    command: npm run dev -- --host

  # --- Redacted - Next.js on 3000 ---
  redacted:
    build:
      context: ./redacted
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - ./redacted/.env
    environment:
      - DATABASE_URL=mongodb://mongodb:27017/redacted
      - NEXT_PUBLIC_APP_DOMAIN=http://localhost:3000
    volumes:
      - ./redacted:/app
      - redacted_node_modules:/app/node_modules
    depends_on:
      - mongodb
    command: sh -c "npm install && npx prisma generate && npm run dev -- --hostname 0.0.0.0"

  # --- MongoDB (for Redacted) ---
  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
  mongodb_data:
  redacted_node_modules: